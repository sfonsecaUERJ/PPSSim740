1) execute command:

cmsRun runPPS_cfg.py
(cmsRun runPPS.cfg  )

the simppsevent.root and
TheAnalysis.root will be produced 

use then:
root -b z1.C
gv z1.ps


2) execute command:
cmsRun runHecMB_cfg.py
(cmsRun runHecMB.cfg)

the HecMBEvent.root will be produced
